package reoseah.rgen;

/**
 * RGen is library of commonly used world generation code from different
 * experiments.
 * 
 * Now in work-in-progress state. Eventually it will be movid in own mod file,
 * available to other mods.
 * 
 * @author Reoseah
 * 
 */
public class RGen {
	/**
	 * If you copied this code into your mod (and renamed package!), this should
	 * be called from the mod preInit method.
	 */
	public static void preInit() {

	}
}
